# apple-mail-mcp
A lightweight, read-only MCP server for Apple Mail. Reads directly from Mail's local SQLite database and .emlx files — no AppleScript needed for reading. Designed for large mailboxes.
